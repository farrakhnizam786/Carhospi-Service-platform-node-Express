<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Sign Up</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include("../partials/navbar") %>

    <section>
      <div class="container" style="max-width: 460px; margin-top: 40px;">
        <h2>User Sign Up</h2>
        <% if (error) { %>
        <p style="color: #f87171; margin: 10px 0;"><%= error %></p>
        <% } %>

        <form action="/signup" method="POST" style="display: grid; gap: 12px; margin-top: 14px;">
          <input type="text" name="name" placeholder="Full Name" required style="padding: 12px; border-radius: 10px; border: 1px solid #334155;" />
          <input type="email" name="email" placeholder="Email" required style="padding: 12px; border-radius: 10px; border: 1px solid #334155;" />
          <input type="password" name="password" placeholder="Password" required style="padding: 12px; border-radius: 10px; border: 1px solid #334155;" />
          <button type="submit" class="btn btn-primary">Sign Up</button>
        </form>

        <p style="margin-top: 14px;">Already have account? <a href="/login" style="color: #22d3ee;">Login</a></p>
      </div>
    </section>
  </body>
</html>
